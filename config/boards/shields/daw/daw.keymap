#include <behaviors.dtsi>

/ {
    keymap {
        compatible = "zmk,keymap";

	default_layer {
            bindings = <
                &kp KP_ASTERISK  &kp ENTER   &bt BT_CLR_ALL
                &kp KP_N0        &kp KP_DOT  &kp K
	    >;
	};
    };
};