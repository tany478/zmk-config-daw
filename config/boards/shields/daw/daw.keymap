#include <behaviors.dtsi>

/ {
    keymap {
        compatible = "zmk,keymap";

	default_layer {
            bindings = <
                &kp KP_ASTERISK  &kp ENTER   &bt BT_SEL 0
                &kp KP_N0        &kp KP_DOT  &kp K
	    >;
	};
    };
};