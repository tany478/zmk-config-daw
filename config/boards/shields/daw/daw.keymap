#include <behaviors.dtsi>

/ {
    keymap {
        compatible = "zmk,keymap";

	default_layer {
            bindings = <
                &kp KP_ASTERISK  &kp ENTER   &kp I
                &kp KP_N0        &kp KP_DOT  &kp K
	    >;
	};
    };
};