#include <behaviors.dtsi>

/ {
    keymap {
        compatible = "zmk,keymap";

	default_layer {
            bindings = <
                &kp KP_MULTIPLY  &kp ENTER   &bt BT_SEL 4
                &kp KP_N0        &kp KP_DOT  &bt BT_CLR_ALL
	    >;
	};
    };
};